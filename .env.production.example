# Production API URL – used when running npm run build.
# Set to your Render backend URL (no trailing slash).
# For Vercel: also set VITE_API_URL in Vercel → Settings → Environment Variables.
VITE_API_URL=https://medibot-backendend.onrender.com
